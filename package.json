{
  "private": true,
  "name": "govuk_admin_template",
  "license": "MIT",
  "devDependencies": {
    "jasmine-browser-runner": "^1.0.0",
    "jasmine-core": "^4.0.1"
  },
  "scripts": {
    "jasmine:prepare": "RAILS_ENV=test bundle exec rake dummy_app:assets:clobber dummy_app:assets:precompile",
    "jasmine:ci": "yarn run jasmine:prepare && yarn run jasmine-browser-runner runSpecs",
    "jasmine:browser": "yarn run jasmine:prepare && yarn run jasmine-browser-runner"
  }
}
